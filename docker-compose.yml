version: "3.8"

services:
  pocketbase:
    build:
      context: .
      args:
        PB_VERSION: "0.23.4"
    ports:
      - "8080:8080"
    volumes:
      - pb_data:/pb/pb_data
    restart: unless-stopped

volumes:
  pb_data:
